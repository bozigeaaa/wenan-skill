# 标题一致性与默认口播化实施计划

**目标：** 让完整口播稿在不固定正文结构的前提下，自动避免把标题扩写为相邻知识，并默认完成不改变语义的强化口语化改写。

**边界：** 不新增用户填写字段，不规定正文顺序，不上传 GitHub。事实、数字、条件、风险边界、公司能力和资料出处保持不变。

### 任务 1：先锁定静态行为校验

- [ ] 在 `tests/test_local_delivery_rules.py` 增加规则连接测试。
- [ ] 验证测试在新规则缺失时失败。

### 任务 2：更新生成与表达规则

- [ ] 在 `AGENTS.md`、文案路由、质量门、Humanizer 审校层和口语化改写 Skill 中写入标题语义一致性与默认强化口播化。
- [ ] 在完整交接文档中同步新流程说明。
- [ ] 不引入固定的正文框架或强制字段。

### 任务 3：验证

- [ ] 运行静态测试，确认规则连接完整。
- [ ] 复核修改范围和 Git 状态；不执行提交或推送。
